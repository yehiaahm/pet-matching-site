# 🚀 دليل التشغيل السريع - موقع مطابقة الحيوانات الأليفة

## ✅ التحقق من أن كل شيء متصل

تم إعداد الموقع بحيث يكون **كل شيء متصل ببعضه البعض** والبيانات **تُخزن بشكل صحيح**.

---

## 📋 المتطلبات

1. **PostgreSQL**: يجب أن يكون مثبتاً ويعمل
2. **Node.js**: الإصدار 16 أو أحدث
3. **قاعدة بيانات**: `pets_db` (يتم إنشاؤها تلقائياً)

---

## 🔧 الإعداد الأولي

### 1. إعداد قاعدة البيانات

```bash
# الدخول إلى مجلد الخادم
cd server

# تثبيت المكتبات
npm install

# تطبيق الهيكل على قاعدة البيانات
npx prisma db push

# (اختياري) إضافة بيانات تجريبية
npx prisma db seed
```

### 2. إعداد Frontend

```bash
# العودة إلى المجلد الرئيسي
cd ..

# تثبيت المكتبات
npm install
```

---

## 🚀 تشغيل الموقع

### الطريقة السريعة (تشغيل كل شيء مرة واحدة)

```bash
# على Windows
START_ALL.bat

# على Linux/Mac
./START_ALL.sh
```

### الطريقة اليدوية

#### 1. تشغيل الخادم (Backend)

```bash
cd server
npm run dev
```

الخادم سيعمل على: `http://localhost:5000`

#### 2. تشغيل Frontend (في نافذة جديدة)

```bash
npm run dev
```

الموقع سيعمل على: `http://localhost:5173`

---

## 🧪 اختبار الاتصال

### اختبار شامل تلقائي

```bash
# على Windows
test-full-integration.bat

# على Linux/Mac
./test-full-integration.sh
```

هذا السكريبت سيتحقق من:
- ✅ اتصال قاعدة البيانات
- ✅ تشغيل الخادم
- ✅ تسجيل مستخدم جديد
- ✅ حفظ البيانات في قاعدة البيانات
- ✅ قراءة البيانات من API

### اختبار يدوي

#### 1. اختبار قاعدة البيانات

```bash
cd server
npx prisma studio
```

سيفتح واجهة لعرض البيانات المخزنة.

#### 2. اختبار API

افتح المتصفح واذهب إلى:
- `http://localhost:5000/api/v1/health` - يجب أن يعرض حالة الخادم
- `http://localhost:5000/api/v1/pets` - يجب أن يعرض قائمة الحيوانات

#### 3. اختبار Frontend

افتح `http://localhost:5173` وجرب:
- التسجيل
- إضافة حيوان أليف
- عرض قائمة الحيوانات
- البحث عن مطابقات

---

## 📊 كيفية التأكد من حفظ البيانات

### عرض إحصائيات قاعدة البيانات

```bash
cd server
node -e "
const { PrismaClient } = require('@prisma/client');
const prisma = new PrismaClient();

async function stats() {
  const users = await prisma.users.count();
  const pets = await prisma.pets.count();
  const requests = await prisma.breeding_requests.count();
  
  console.log('📊 إحصائيات قاعدة البيانات:');
  console.log('  👥 المستخدمين:', users);
  console.log('  🐕 الحيوانات:', pets);
  console.log('  💕 طلبات التزاوج:', requests);
  
  await prisma.\$disconnect();
}

stats();
"
```

---

## 🔗 الاتصال بين المكونات

### Frontend ↔️ Backend

- **Frontend** يستخدم `http://localhost:5000/api/v1`
- **ملف API موحد**: `src/lib/api.ts`
- **جميع الطلبات** تمر عبر دالة `apiFetch` التي تتعامل مع:
  - إضافة التوكن تلقائياً
  - معالجة الأخطاء
  - تسجيل الطلبات

### Backend ↔️ Database

- **الخادم** يستخدم Prisma للاتصال بـ PostgreSQL
- **قاعدة البيانات**: `pets_db`
- **جميع العمليات** تُحفظ تلقائياً في قاعدة البيانات

---

## 📁 ملفات الإعدادات المهمة

### 1. إعدادات Frontend (`.env`)
```env
VITE_API_BASE=http://localhost:5000/api/v1
DATABASE_URL="postgresql://postgres:yehia.hema195200@localhost:5432/pets_db"
```

### 2. إعدادات Backend (`server/.env`)
```env
PORT=5000
DATABASE_URL="postgresql://postgres:yehia.hema195200@localhost:5432/pets_db"
JWT_SECRET="dev-jwt-access-secret-123456789"
```

---

## 🔍 استكشاف الأخطاء

### المشكلة: الخادم لا يعمل

```bash
# تحقق من أن المنفذ 5000 غير مستخدم
netstat -ano | findstr :5000

# أو استخدم منفذ آخر في server/.env
PORT=5001
```

### المشكلة: قاعدة البيانات غير متصلة

```bash
# تحقق من تشغيل PostgreSQL
# على Windows: افتح Services وابحث عن PostgreSQL

# على Linux/Mac:
sudo systemctl status postgresql

# اختبر الاتصال
cd server
npx prisma db pull
```

### المشكلة: البيانات لا تُحفظ

```bash
# تحقق من Schema
cd server
npx prisma generate
npx prisma db push

# اعرض logs الخادم
npm run dev
# شاهد الأخطاء في Console
```

---

## 📚 الموارد الإضافية

- **API Documentation**: `ADMIN_API_REFERENCE.md`
- **Database Schema**: `server/prisma/schema.prisma`
- **Architecture**: `ARCHITECTURE.md`
- **Admin Guide**: `ADMIN_QUICK_REFERENCE_AR.md`

---

## ✨ المميزات المتصلة

1. **التسجيل والدخول** ← يُحفظ في جدول `users`
2. **إضافة حيوان** ← يُحفظ في جدول `pets`
3. **طلبات التزاوج** ← يُحفظ في جدول `breeding_requests`
4. **الرسائل** ← يُحفظ في جدول `messages`
5. **التقييمات** ← يُحفظ في جدول `reviews`
6. **الحجوزات** ← يُحفظ في جدول `clinic_bookings`

---

## 🎯 الخلاصة

✅ **Frontend** متصل بـ **Backend** عبر `http://localhost:5000/api/v1`
✅ **Backend** متصل بـ **PostgreSQL** عبر Prisma
✅ **جميع البيانات** تُحفظ في قاعدة البيانات `pets_db`
✅ **API موحد** في `src/lib/api.ts` لسهولة الصيانة

---

## 💡 نصائح

1. استخدم `npx prisma studio` لعرض البيانات المخزنة
2. افحص Console في المتصفح لرؤية طلبات API
3. افحص logs الخادم لرؤية الاستعلامات
4. استخدم `test-full-integration.bat` للاختبار السريع

---

**تم إعداد كل شيء ليكون متصلاً ومتزامناً! 🎉**
